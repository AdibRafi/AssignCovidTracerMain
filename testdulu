CLOSE

-go to MasterFileAdmin,
-search for the same Date -> Shop -> !name(x nak Adib) : then put it inside possibleCase(take the time)
-then calculate the time Diff so in this context 1 hour before n 1 hour after the possibleCase
- make another arrayList<> closeName
-
-after u calculate, do the same scan of the file, but this time cari the same time inside the timediff.
-if you find the .equal with the timeDiff, add it the name into closeName

now go back to CustomerFileAdmin
- Scan the file until .equal(closeName)
- make sure the only put "CLOSE" when !"CASE"

also make sure to rewrite the file